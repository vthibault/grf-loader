name: CI

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: install
        run: yarn install

      - name: Run linter
        run: yarn lint

      - name: Run tests
        run: yarn test
